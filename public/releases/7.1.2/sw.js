const CACHE='monthly-expense-v7-1-2-release-gate-push';
const SHELL_KEY='/__monthly_expense_app_shell__';
const SW_VERSION='7.1.2';
const STATIC_CORE=['/manifest.webmanifest','/icon-192.png','/icon-512.png','/icon-maskable-512.png','/apple-touch-icon.png','/release-manager.js?v=7.1.2'];
function isSameOrigin(url){return url.origin===self.location.origin}
async function normalizedResponse(res){const body=await res.arrayBuffer(),headers=new Headers(res.headers);headers.delete('content-length');headers.delete('content-encoding');headers.delete('location');headers.set('cache-control','no-store');return new Response(body,{status:200,statusText:'OK',headers})}
async function fetchFreshShell(){const res=await fetch(`/?__app_shell=${Date.now()}`,{cache:'no-store',redirect:'follow',credentials:'include'});if(!res.ok)throw new Error(`Shell HTTP ${res.status}`);return normalizedResponse(res)}
async function refreshShell(cache){const clean=await fetchFreshShell();await cache.put(SHELL_KEY,clean.clone());return clean}
async function cacheStatic(cache){for(const path of STATIC_CORE){try{const res=await fetch(`${path}${path.includes('?')?'&':'?'}__static=${Date.now()}`,{cache:'no-store',redirect:'follow'});if(res.ok)await cache.put(path,res.clone())}catch(_){}}}
self.addEventListener('install',e=>{e.waitUntil((async()=>{const cache=await caches.open(CACHE);await Promise.allSettled([refreshShell(cache),cacheStatic(cache)])})())});
self.addEventListener('message',e=>{if(e.data&&e.data.type==='SKIP_WAITING'){self.skipWaiting();return}if(e.data&&e.data.type==='REFRESH_APP_SHELL'){const port=e.ports&&e.ports[0];e.waitUntil((async()=>{try{const cache=await caches.open(CACHE);await refreshShell(cache);await cacheStatic(cache);port?.postMessage({ok:true,version:SW_VERSION})}catch(err){port?.postMessage({ok:false,error:String(err?.message||err)})}})())}});
self.addEventListener('activate',e=>{e.waitUntil(Promise.all([caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('monthly-expense-v')&&k!==CACHE).map(k=>caches.delete(k)))),self.clients.claim()]))});
self.addEventListener('fetch',e=>{const r=e.request;if(r.method!=='GET')return;const u=new URL(r.url);if(isSameOrigin(u)&&u.pathname.startsWith('/api/'))return;if(isSameOrigin(u)&&(u.searchParams.has('__update_check')||u.searchParams.has('__sw_recovery')||u.searchParams.has('__raw_base')))return;if(!isSameOrigin(u))return;const isNav=r.mode==='navigate'||u.pathname==='/'||u.pathname==='/index.html';if(isNav){e.respondWith((async()=>{const cache=await caches.open(CACHE),shell=await cache.match(SHELL_KEY);if(shell)return shell;try{return await refreshShell(cache)}catch(_){return normalizedResponse(await fetch(r,{cache:'no-store',redirect:'follow',credentials:'include'}))}})());return}e.respondWith((async()=>{const cache=await caches.open(CACHE),hit=await cache.match(r);if(hit)return hit;const res=await fetch(r);if(res.ok||res.type==='opaque')cache.put(r,res.clone()).catch(()=>{});return res})())});
async function updateStatus(){try{const u=new URL('/api/update/status',self.location.origin);u.searchParams.set('current',SW_VERSION);u.searchParams.set('source','push');const r=await fetch(u,{cache:'no-store',credentials:'include'});return r.ok?await r.json():null}catch(_){return null}}
self.addEventListener('push',e=>{e.waitUntil((async()=>{const s=await updateStatus();if(!s?.ok)return;const v=s.availableVersion||s.stableVersion||'';if(!v||v===SW_VERSION)return;const notes=s.candidateNotes?.ar||'';await self.registration.showNotification('تحديث جديد متوفر',{body:`الإصدار ${v} متوفر الآن.${notes?` ${notes}`:''}`,icon:'/icon-192.png',badge:'/icon-192.png',tag:`monthly-expense-update-${v}`,renotify:true,requireInteraction:false,data:{url:`/?update=${encodeURIComponent(v)}`,version:v}})})())});
self.addEventListener('notificationclick',e=>{e.notification.close();e.waitUntil((async()=>{const target=new URL(e.notification.data?.url||'/',self.location.origin).href,clientsList=await self.clients.matchAll({type:'window',includeUncontrolled:true});for(const c of clientsList){if(new URL(c.url).origin===self.location.origin){try{await c.focus();c.postMessage({type:'OPEN_UPDATE',version:e.notification.data?.version||''});return}catch(_){}}}await self.clients.openWindow(target)})())});
function b64ToBytes(v){const s=String(v||'').replace(/-/g,'+').replace(/_/g,'/'),pad='='.repeat((4-s.length%4)%4),raw=atob(s+pad);return Uint8Array.from(raw,c=>c.charCodeAt(0))}
self.addEventListener('pushsubscriptionchange',e=>{e.waitUntil((async()=>{try{const kr=await fetch('/api/update/push-key',{cache:'no-store',credentials:'include'}),kj=await kr.json();if(!kr.ok||!kj.publicKey)return;const sub=await self.registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64ToBytes(kj.publicKey)});await fetch('/api/update/subscribe',{method:'POST',credentials:'include',headers:{'content-type':'application/json','x-monthly-expense-app':'1'},body:JSON.stringify({subscription:sub.toJSON()})})}catch(_){}})())});
